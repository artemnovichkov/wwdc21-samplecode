/*
See LICENSE folder for this sample’s licensing information.

Abstract:
The initial view that the app loads.
*/

import SwiftUI

struct ContentView: View {
    var body: some View {
        ScenarioList()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
